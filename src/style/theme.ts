import { createTheme } from "@nextui-org/react"

export const theme = createTheme({
  type: "light",
  theme: {
    fonts: {
      sans: "Noto Sans KR, sans-serif",
    },
  },
})

export const COLORS = {
  primary: "#0072F5",
}
